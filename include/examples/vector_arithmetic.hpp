#ifndef VECTOR_ARITHMETIC_HPP
#define VECTOR_ARITHMETIC_HPP

#include <cstddef>

void vecAdd(int* c, int* a, int* b, size_t count);
void vecMul(int* c, int* a, int* b, size_t count);

#endif  // VECTOR_ARITHMETIC_HPP